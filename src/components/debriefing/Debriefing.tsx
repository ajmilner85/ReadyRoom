import React from 'react';
import MissionDebriefing from './MissionDebriefing';

/**
 * Main entry point for the Debriefing feature
 * Renders the MissionDebriefing component which handles all debriefing workflows
 */
const Debriefing: React.FC = () => {
  return <MissionDebriefing />;
};

export default Debriefing;
